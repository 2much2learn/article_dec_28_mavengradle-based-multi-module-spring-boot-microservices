name: Análisis de dependencias con SonarQube

on:
  push:
    branches:
      - feature-ms-davidsilva-cicd

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código fuente
        uses: actions/checkout@v2

      - name: Levantar contenedor de SonarQube
        run: |
          docker run -d --name sonarqube -p 9000:9000 sonarqube:latest

      - name: Analizar dependencias
        run: |
          # Ejecutar el análisis de dependencias aquí

